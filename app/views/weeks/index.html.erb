<p style="color: green"><%= notice %></p>

<h1>Weeks</h1>


<% if is_board? %>
  <%= link_to "New week", new_week_path %>
<% end %>

<div id="weeks">
  <% @weeks.each do |week| %>
    <%= render week %>
    <p>
      <%= link_to "Show this week", week %>
    </p>
  <% end %>
</div>


